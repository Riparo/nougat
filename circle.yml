machine:
  python:
    version: 3.5.1

dependencies:
  pre:
    - pip install pytest httptools uvloop aiohttp aiofiles pytest-cov
    - python setup.py install

test:
  override:
    - pytest --cov=misuzu